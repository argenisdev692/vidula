/** Shared types for the reusable form kit (common/form). */

/** A single option for SelectField / MultiSelect-style controls. */
export interface SelectOption {
    label: string;
    value: string;
}
